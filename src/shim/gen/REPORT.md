# Generated thunks: what was emitted, and what was refused

GENERATED by `gen_thunks.py` (#78, #82). Do not edit — regenerate with `./build.sh`.

Every method below is one the emitter **declined**, with the reason. A refused
slot keeps its logging stub, so a title that calls one still names it in
`shim-unix.log` the first time it does (#45/#46) — the refusal is loud at build
time and loud again at runtime, never silent.

A refusal can be PARTIAL. The unit of generation is (interface, method,
signature), so a method whose older versions Proton hand-writes and whose newer
ones carry a readable signature is generated for the second set and refused for
the first — `ISteamMatchmakingServers::RequestInternetServerList` is refused for
2 versions and generated for the rest. The versions column is that count.

## Totals

| | |
| --- | --- |
| interface versions | 212 |
| vtable slots | 6555 |
| slots wired to a generated thunk | 5191 |
| — of those, x86_64 only | 110 |
| distinct shapes emitted | 1143 |
| — of those, x86_64 only | 22 |
| methods refused | 163 |

## x86_64-only shapes

A shape is generated for the 64-bit build alone when its correctness depends on
pointers being 8 bytes wide on both sides of the seam — an array of pointers, or a
struct Proton states is identical on x86_64 and divergent on i386. On the 32-bit
build those slots keep their stub and report themselves the first time a title
calls one, which is a named line rather than a wrong answer.

## Per interface

| interface | shapes emitted | x86_64 only | methods refused |
| --- | ---: | ---: | ---: |
| `ISteamAppList` | 5 | 0 | 0 |
| `ISteamAppTicket` | 1 | 0 | 0 |
| `ISteamApps` | 26 | 0 | 12 |
| `ISteamBilling` | 43 | 0 | 0 |
| `ISteamClient` | 10 | 0 | 43 |
| `ISteamController` | 41 | 0 | 2 |
| `ISteamFriends` | 99 | 0 | 9 |
| `ISteamGameCoordinator` | 3 | 0 | 0 |
| `ISteamGameSearch` | 14 | 0 | 0 |
| `ISteamGameServer` | 80 | 0 | 0 |
| `ISteamGameServerStats` | 10 | 0 | 0 |
| `ISteamGameStats` | 13 | 0 | 0 |
| `ISteamHTMLSurface` | 39 | 1 | 0 |
| `ISteamHTTP` | 25 | 0 | 0 |
| `ISteamInput` | 40 | 0 | 11 |
| `ISteamInventory` | 40 | 0 | 0 |
| `ISteamMasterServerUpdater` | 14 | 0 | 0 |
| `ISteamMatchmaking` | 59 | 0 | 0 |
| `ISteamMatchmakingServers` | 19 | 5 | 9 |
| `ISteamMusic` | 9 | 0 | 0 |
| `ISteamMusicRemote` | 32 | 0 | 0 |
| `ISteamNetworking` | 34 | 0 | 0 |
| `ISteamNetworkingFakeUDPPort` | 2 | 0 | 2 |
| `ISteamNetworkingMessages` | 5 | 0 | 1 |
| `ISteamNetworkingSockets` | 59 | 0 | 8 |
| `ISteamNetworkingSocketsSerialized` | 9 | 0 | 0 |
| `ISteamNetworkingUtils` | 27 | 1 | 2 |
| `ISteamParentalSettings` | 6 | 0 | 0 |
| `ISteamParties` | 8 | 0 | 4 |
| `ISteamRemotePlay` | 21 | 0 | 0 |
| `ISteamRemoteStorage` | 43 | 10 | 24 |
| `ISteamScreenshots` | 9 | 0 | 0 |
| `ISteamTimeline` | 21 | 0 | 0 |
| `ISteamUGC` | 107 | 3 | 1 |
| `ISteamUnifiedMessages` | 5 | 0 | 0 |
| `ISteamUser` | 82 | 0 | 6 |
| `ISteamUserStats` | 52 | 2 | 11 |
| `ISteamUtils` | 27 | 0 | 18 |
| `ISteamVideo` | 4 | 0 | 0 |

## Refusals

| interface | method | versions | reason |
| --- | --- | ---: | --- |
| `ISteamApps` | `BIsCybercafe` | 8 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamApps` | `BIsDlcInstalled` | 7 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamApps` | `BIsLowViolence` | 8 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamApps` | `BIsSubscribed` | 8 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamApps` | `BIsSubscribedApp` | 8 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamApps` | `BIsSubscribedFromFreeWeekend` | 6 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamApps` | `BIsVACBanned` | 8 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamApps` | `GetAppOwner` | 4 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamApps` | `GetAvailableGameLanguages` | 8 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamApps` | `GetCurrentGameLanguage` | 8 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamApps` | `GetEarliestPurchaseUnixTime` | 6 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamApps` | `GetLaunchQueryParam` | 4 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamClient` | `BReleaseSteamPipe` | 17 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamClient` | `ConnectToGlobalUser` | 17 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamClient` | `CreateSteamPipe` | 17 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamClient` | `DEPRECATED_GetISteamUnifiedMessages` | 4 | returns `void *`, a native pointer — a 32-bit PE cannot hold a macOS heap address, and there is no length to copy down by |
| `ISteamClient` | `DEPRECATED_Remove_SteamAPI_CPostAPIResultInProcess` | 6 | function-pointer parameter `void (*)(void)` — calling it means a deferred upcall from native code back into PE code, which the seam does not carry |
| `ISteamClient` | `DEPRECATED_Set_SteamAPI_CPostAPIResultInProcess` | 6 | function-pointer parameter `void (*)(void)` — calling it means a deferred upcall from native code back into PE code, which the seam does not carry |
| `ISteamClient` | `GetISteamAppList` | 8 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamClient` | `GetISteamApps` | 17 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamClient` | `GetISteamBilling` | 1 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamClient` | `GetISteamContentServer` | 2 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamClient` | `GetISteamController` | 11 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamClient` | `GetISteamFriends` | 17 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamClient` | `GetISteamGameSearch` | 4 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamClient` | `GetISteamGameServer` | 17 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamClient` | `GetISteamGameServerStats` | 14 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamClient` | `GetISteamGenericInterface` | 16 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamClient` | `GetISteamHTMLSurface` | 7 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamClient` | `GetISteamHTTP` | 13 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamClient` | `GetISteamInput` | 5 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamClient` | `GetISteamInventory` | 7 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamClient` | `GetISteamMasterServerUpdater` | 6 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamClient` | `GetISteamMatchmaking` | 17 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamClient` | `GetISteamMatchmakingServers` | 17 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamClient` | `GetISteamMusic` | 9 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamClient` | `GetISteamMusicRemote` | 7 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamClient` | `GetISteamNetworking` | 16 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamClient` | `GetISteamParentalSettings` | 6 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamClient` | `GetISteamParties` | 5 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamClient` | `GetISteamRemotePlay` | 4 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamClient` | `GetISteamRemoteStorage` | 16 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamClient` | `GetISteamScreenshots` | 12 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamClient` | `GetISteamUGC` | 11 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamClient` | `GetISteamUnifiedMessages` | 5 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamClient` | `GetISteamUser` | 17 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamClient` | `GetISteamUserStats` | 16 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamClient` | `GetISteamUtils` | 17 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamClient` | `GetISteamVideo` | 7 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamClient` | `GetIVAC` | 1 | returns `void *`, a native pointer — a 32-bit PE cannot hold a macOS heap address, and there is no length to copy down by |
| `ISteamClient` | `ReleaseUser` | 17 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamClient` | `Remove_SteamAPI_CPostAPIResultInProcess` | 1 | function-pointer parameter `void (*)(uint64_t, void *, uint32_t, int32_t)` — calling it means a deferred upcall from native code back into PE code, which the seam does not carry |
| `ISteamClient` | `SetWarningMessageHook` | 16 | function-pointer parameter `void (*)(int32_t, const char *)` — calling it means a deferred upcall from native code back into PE code, which the seam does not carry |
| `ISteamClient` | `Set_SteamAPI_CCheckCallbackRegisteredInProcess` | 7 | function-pointer parameter `uint32_t (*)(int32_t)` — calling it means a deferred upcall from native code back into PE code, which the seam does not carry |
| `ISteamClient` | `Set_SteamAPI_CPostAPIResultInProcess` | 1 | function-pointer parameter `void (*)(uint64_t, void *, uint32_t, int32_t)` — calling it means a deferred upcall from native code back into PE code, which the seam does not carry |
| `ISteamController` | `GetGlyphForActionOrigin` | 4 | Proton hand-writes this wrapper — there is no one-line typed signature to read |
| `ISteamController` | `GetGlyphForXboxOrigin` | 2 | Proton hand-writes this wrapper — there is no one-line typed signature to read |
| `ISteamFriends` | `ActivateGameOverlay` | 15 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamFriends` | `ActivateGameOverlayInviteDialog` | 12 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamFriends` | `ActivateGameOverlayInviteDialogConnectString` | 2 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamFriends` | `ActivateGameOverlayRemotePlayTogetherInviteDialog` | 2 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamFriends` | `ActivateGameOverlayToStore` | 13 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamFriends` | `ActivateGameOverlayToUser` | 13 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamFriends` | `ActivateGameOverlayToWebPage` | 13 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamFriends` | `GetPersonaName` | 17 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamFriends` | `RegisterProtocolInOverlayBrowser` | 2 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamInput` | `BNewDataAvailable` | 3 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamInput` | `EnableActionEventCallbacks` | 3 | function-pointer parameter `void (*)(SteamInputActionEvent_t *)` — calling it means a deferred upcall from native code back into PE code, which the seam does not carry |
| `ISteamInput` | `GetConnectedControllers` | 5 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamInput` | `GetGlyphForActionOrigin` | 2 | Proton hand-writes this wrapper — there is no one-line typed signature to read |
| `ISteamInput` | `GetGlyphForActionOrigin_Legacy` | 3 | Proton hand-writes this wrapper — there is no one-line typed signature to read |
| `ISteamInput` | `GetGlyphForXboxOrigin` | 5 | Proton hand-writes this wrapper — there is no one-line typed signature to read |
| `ISteamInput` | `GetGlyphPNGForActionOrigin` | 3 | Proton hand-writes this wrapper — there is no one-line typed signature to read |
| `ISteamInput` | `GetGlyphSVGForActionOrigin` | 3 | Proton hand-writes this wrapper — there is no one-line typed signature to read |
| `ISteamInput` | `Init` | 5 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamInput` | `RunFrame` | 5 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamInput` | `Shutdown` | 5 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamMatchmakingServers` | `CancelQuery` | 3 | Proton hand-writes this wrapper — there is no one-line typed signature to read |
| `ISteamMatchmakingServers` | `GetServerDetails` | 3 | returns `gameserveritem_t_105 *`, a native pointer — a 32-bit PE cannot hold a macOS heap address, and there is no length to copy down by |
| `ISteamMatchmakingServers` | `ReleaseRequest` | 2 | Proton hand-writes this wrapper — there is no one-line typed signature to read |
| `ISteamMatchmakingServers` | `RequestFavoritesServerList` | 2 | Proton hand-writes this wrapper — there is no one-line typed signature to read |
| `ISteamMatchmakingServers` | `RequestFriendsServerList` | 2 | Proton hand-writes this wrapper — there is no one-line typed signature to read |
| `ISteamMatchmakingServers` | `RequestHistoryServerList` | 2 | Proton hand-writes this wrapper — there is no one-line typed signature to read |
| `ISteamMatchmakingServers` | `RequestInternetServerList` | 2 | Proton hand-writes this wrapper — there is no one-line typed signature to read |
| `ISteamMatchmakingServers` | `RequestLANServerList` | 2 | Proton hand-writes this wrapper — there is no one-line typed signature to read |
| `ISteamMatchmakingServers` | `RequestSpectatorServerList` | 2 | Proton hand-writes this wrapper — there is no one-line typed signature to read |
| `ISteamNetworkingFakeUDPPort` | `DestroyFakeUDPPort` | 1 | Proton hand-writes this wrapper — there is no one-line typed signature to read |
| `ISteamNetworkingFakeUDPPort` | `ReceiveMessages` | 1 | Proton hand-writes this wrapper — there is no one-line typed signature to read |
| `ISteamNetworkingMessages` | `ReceiveMessagesOnChannel` | 1 | Proton hand-writes this wrapper — there is no one-line typed signature to read |
| `ISteamNetworkingSockets` | `CreateFakeUDPPort` | 2 | returns `void *`, a native pointer — a 32-bit PE cannot hold a macOS heap address, and there is no length to copy down by |
| `ISteamNetworkingSockets` | `GetCertificateRequest` | 4 | function-pointer parameter `char (*)[1024]` — calling it means a deferred upcall from native code back into PE code, which the seam does not carry |
| `ISteamNetworkingSockets` | `ReceiveMessagesOnConnection` | 7 | Proton hand-writes this wrapper — there is no one-line typed signature to read |
| `ISteamNetworkingSockets` | `ReceiveMessagesOnListenSocket` | 3 | Proton hand-writes this wrapper — there is no one-line typed signature to read |
| `ISteamNetworkingSockets` | `ReceiveMessagesOnPollGroup` | 4 | Proton hand-writes this wrapper — there is no one-line typed signature to read |
| `ISteamNetworkingSockets` | `RunCallbacks` | 3 | Proton hand-writes this wrapper — there is no one-line typed signature to read |
| `ISteamNetworkingSockets` | `SendMessages` | 5 | Proton hand-writes this wrapper — there is no one-line typed signature to read |
| `ISteamNetworkingSockets` | `SetCertificate` | 4 | function-pointer parameter `char (*)[1024]` — calling it means a deferred upcall from native code back into PE code, which the seam does not carry |
| `ISteamNetworkingUtils` | `AllocateMessage` | 2 | Proton hand-writes this wrapper — there is no one-line typed signature to read |
| `ISteamNetworkingUtils` | `SetDebugOutputFunction` | 4 | function-pointer parameter `void (*)(uint32_t, const char *)` — calling it means a deferred upcall from native code back into PE code, which the seam does not carry |
| `ISteamParties` | `CreateBeacon` | 1 | parameter points at `SteamPartyBeaconLocation_t`, whose Windows and unix layouts differ on x86_64 by Proton's own generated definitions — it needs a field-by-field converter, and the element count and direction are not in the signature |
| `ISteamParties` | `GetAvailableBeaconLocations` | 1 | parameter points at `SteamPartyBeaconLocation_t`, whose Windows and unix layouts differ on x86_64 by Proton's own generated definitions — it needs a field-by-field converter, and the element count and direction are not in the signature |
| `ISteamParties` | `GetBeaconDetails` | 1 | parameter points at `SteamPartyBeaconLocation_t`, whose Windows and unix layouts differ on x86_64 by Proton's own generated definitions — it needs a field-by-field converter, and the element count and direction are not in the signature |
| `ISteamParties` | `GetBeaconLocationData` | 1 | by-value parameter `SteamPartyBeaconLocation_t`, whose Windows and unix layouts differ on x86_64 by Proton's own generated definitions |
| `ISteamRemoteStorage` | `FileDelete` | 14 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamRemoteStorage` | `FileExists` | 15 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamRemoteStorage` | `FileForget` | 13 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamRemoteStorage` | `FilePersisted` | 13 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamRemoteStorage` | `FileRead` | 15 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamRemoteStorage` | `FileReadAsync` | 3 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamRemoteStorage` | `FileReadAsyncComplete` | 3 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamRemoteStorage` | `FileShare` | 13 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamRemoteStorage` | `FileWrite` | 15 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamRemoteStorage` | `FileWriteAsync` | 3 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamRemoteStorage` | `FileWriteStreamCancel` | 8 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamRemoteStorage` | `FileWriteStreamClose` | 8 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamRemoteStorage` | `FileWriteStreamOpen` | 8 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamRemoteStorage` | `FileWriteStreamWriteChunk` | 8 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamRemoteStorage` | `GetFileCount` | 15 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamRemoteStorage` | `GetFileNameAndSize` | 15 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamRemoteStorage` | `GetFileSize` | 15 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamRemoteStorage` | `GetFileTimestamp` | 13 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamRemoteStorage` | `GetQuota` | 15 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamRemoteStorage` | `GetSyncPlatforms` | 12 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamRemoteStorage` | `IsCloudEnabledForAccount` | 13 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamRemoteStorage` | `IsCloudEnabledForApp` | 13 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamRemoteStorage` | `SetCloudEnabledForApp` | 13 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamRemoteStorage` | `SetSyncPlatforms` | 12 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamUGC` | `GetQueryUGCResult` | 19 | parameter points at `SteamUGCDetails_t_126`, whose Windows and unix layouts differ on x86_64 by Proton's own generated definitions — it needs a field-by-field converter, and the element count and direction are not in the signature |
| `ISteamUser` | `BLoggedOn` | 20 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamUser` | `GetEncryptedAppTicket` | 10 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamUser` | `GetHSteamUser` | 20 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamUser` | `GetSteamID` | 20 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamUser` | `GetUserDataFolder` | 13 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamUser` | `RequestEncryptedAppTicket` | 10 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamUserStats` | `ClearAchievement` | 13 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamUserStats` | `GetAchievement` | 13 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamUserStats` | `GetAchievementAndUnlockTime` | 7 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamUserStats` | `GetAchievementDisplayAttribute` | 13 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamUserStats` | `GetAchievementName` | 5 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamUserStats` | `GetDownloadedLeaderboardEntry` | 3 | parameter points at `LeaderboardEntry_t_123`, whose Windows and unix layouts differ on x86_64 by Proton's own generated definitions — it needs a field-by-field converter, and the element count and direction are not in the signature |
| `ISteamUserStats` | `GetNumAchievements` | 5 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamUserStats` | `RequestCurrentStats` | 12 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamUserStats` | `ResetAllStats` | 9 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamUserStats` | `SetAchievement` | 13 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamUserStats` | `StoreStats` | 13 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamUtils` | `BOverlayNeedsPresent` | 7 | semantic override: renderer state, not client state — forwarded to the renderer's own export so the answer is correct by construction: no renderer loaded, no symbol, false (#23). |
| `ISteamUtils` | `GetAPICallFailureReason` | 9 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamUtils` | `GetAPICallResult` | 9 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamUtils` | `GetAppID` | 9 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamUtils` | `GetConnectedUniverse` | 9 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamUtils` | `GetCurrentBatteryPower` | 9 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamUtils` | `GetIPCCallCount` | 8 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamUtils` | `GetIPCountry` | 9 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamUtils` | `GetSecondsSinceAppActive` | 9 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamUtils` | `GetSecondsSinceComputerActive` | 9 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamUtils` | `GetServerRealTime` | 9 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamUtils` | `GetSteamUILanguage` | 6 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamUtils` | `IsAPICallCompleted` | 9 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamUtils` | `IsOverlayEnabled` | 8 | semantic override: answers ARMED, never loaded. Blind forwarding is correct code and wrong behaviour: a title told `true` with no renderer armed pauses forever waiting for a panel that never comes. Answered by Valve's renderer, not by steamclient.dylib (#23). |
| `ISteamUtils` | `RunFrame` | 8 | hand-written: a hand-written thunk in shim_pe.c already serves this method; generating a second one would race it into the same slot |
| `ISteamUtils` | `SetOverlayNotificationInset` | 5 | semantic override: the renderer exports no inset setter, so this is accept-and-ignore by necessity; forwarding it to the client would be a call to nothing (#23). |
| `ISteamUtils` | `SetOverlayNotificationPosition` | 9 | semantic override: renderer state, not client state — forwarded to the renderer's own export (#23). |
| `ISteamUtils` | `SetWarningMessageHook` | 8 | semantic override: installs a PE callback the native client would call back INTO, which the seam does not carry — and deliberately not forwarded even so, for the same reason as IsOverlayEnabled: it is a semantic, not a marshalling, decision. |

